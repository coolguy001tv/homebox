{
  // https://v3.nuxtjs.org/concepts/typescript
  "extends": "./.nuxt/tsconfig.json",
  "compilerOptions": {
    // 说明：typecheck 现在使用本地 vue-tsc + typescript（不再经 npx 拉 latest），
    // 因此不再需要 ignoreDeprecations 压制新版 TS 的弃用警告。
  }
}
